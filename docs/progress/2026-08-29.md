# BranchScript progress — 2026-08-29

## Public repository boundary

- [x] Moved cloud backend implementation and infrastructure configuration to a private repository.
- [x] Kept the public subdomain and browser API paths unchanged.
- [x] Reduced the public Compose project to the static `branchscript` web container.
- [x] Removed backend build jobs, environment templates, and private operations documentation from the public repository.
- [x] Kept the local-first editor, scripting language, playground, IndexedDB storage, and export workflow public.
- [x] Preserved cloud features through the same-origin browser API contract without exposing backend implementation.

## Validation

- `npm run check`: passed.
- `docker compose config --quiet`: passed for the static web service.
- Public repository scan found no backend source, migration, credential template, or private gateway configuration.

## Canvas interaction and localization

- [x] Separated wheel panning from modified-wheel zoom so `Command/Ctrl + wheel` only zooms.
- [x] Kept unmodified mouse-wheel and trackpad scrolling available for canvas panning.
- [x] Added a bounded right-click menu for empty-canvas and node-specific actions.
- [x] Connected node actions to the visual builder and matching source selection.
- [x] Added English, Turkish, Spanish, German, and French interface options.
- [x] Kept `.mtree` keywords, scripts, diagram labels, and other user content language-neutral.
- [x] Added translated dynamic statuses, inspector text, runner prompts, and accessible labels.
- [x] Added unit and browser coverage for localization, context actions, zoom anchoring, and mobile overflow.
- [x] Replaced native browser confirmations with an in-app confirmation bubble.
- [x] Added context-specific replace, open, and delete confirmation actions with focus restoration and Escape cancellation.
- [x] Localized confirmation content across all five interface languages.
- [x] Disabled browser text selection across application chrome, playground cards, canvas labels, and status panels.
- [x] Kept text selection enabled inside the BranchScript editor and editable form controls.
- [x] Blocked page-wide `Command/Ctrl + A` when focus is outside an editable control.
- [x] Changed Live Run reset to restart directly from the sole root node instead of returning to a redundant start prompt.
- [x] Kept multi-root diagrams on the start-choice screen after reset and preserved Stop run as the explicit way to close the runner.

## Validation — canvas and languages

- `npm run lint`: passed.
- `npm run typecheck`: passed.
- `npm run test`: 18 tests passed.
- `npm run check`: passed after the canvas and localization changes; 18 unit tests passed.
- `npm run test:e2e`: 13 browser tests passed, including selection boundaries, confirmation accept/cancel, and localized mobile overflow coverage.
- Desktop browser inspection confirmed the language selector fits the top bar and the graph remains readable.
- Browser inspection confirmed the confirmation bubble has clear contrast, compact spacing, and leaves the current script untouched when cancelled.
- Browser inspection confirmed application text remains unselected while CodeMirror keeps its normal selection behavior.
- Live Run browser coverage confirms Reset restarts at the first node and Stop run closes the runner.

## Interview preparation content

- [x] Added `@text`, `@answer`, and `@feature` content attributes to the graph model and `.mtree` compiler.
- [x] Extended source ranges so selecting a box also highlights its content attributes in the editor.
- [x] Added supporting text, prepared answer, and relevant-property controls to the visual builder.
- [x] Rendered recall-ready content inside cards, decisions, neural nodes, and data-structure shapes.
- [x] Added readable full content to the node inspector and active Live Run step.
- [x] Assigned a semantic feature to every view: follow-up cue, expected result, activation, branch rule, complexity, or operation.
- [x] Expanded the interview example with introductions, ownership, architecture, collaboration, technology, incident, result, and likely follow-up paths.
- [x] Expanded all six playground presets with view-relevant descriptions and properties.
- [x] Documented rich content syntax in the in-app guide, README, and language reference.
- [x] Localized new builder fields and semantic captions across English, Turkish, Spanish, German, and French.

## View-specific Live Run

- [x] Added a distinct runner palette and motion language for every diagram type.
- [x] Trees grow into branches; flows move forward; neural maps pulse; logic flashes decisions; algorithms scan in steps; data structures shift like cells.
- [x] Synchronized active-node and active-edge animation with the Live Run path.
- [x] Preserved a reduced-motion mode for accessibility.
- [x] Fixed X6 text reference points so wrapped answers and features stay inside every shape.

## Validation — interview content and Live Run

- `npm run check`: passed with 20 unit tests after the final visual-reference adjustment.
- `npm run test:e2e`: 15 browser tests passed, including rich-card bounds and all six Live Run animation modes.
- Desktop visual checks confirmed prepared answers remain inside cards, decisions, neural nodes, and data shapes.
- Mobile visual check at 390×844 confirmed the responsive Canvas workspace remains within the viewport.

## Translucent light theme

- [x] Replaced opaque white component surfaces with lightly tinted translucent layers.
- [x] Added restrained blur, saturation, inset highlights, and green-gray shadows to controls and floating panels.
- [x] Kept the editor and workspace contrast readable over a soft GPT-inspired background treatment.
- [x] Changed graph shapes to translucent light bodies while preserving their semantic stroke colors.
- [x] Added light runner tints for all six Live Run modes.
- [x] Verified the treatment at desktop and 390×844 mobile sizes.
- `npm run check`: passed with 20 unit tests.
- `npm run test:e2e`: 16 browser tests passed, including translucent control and graph-node coverage.

## Adjustable source panel

- [x] Added a source-panel close control and a visible restore action in the canvas toolbar.
- [x] Added a draggable desktop separator with pointer capture and bounded canvas/editor widths.
- [x] Added Arrow key, Home, End, and double-click controls for accessible resizing and reset.
- [x] Persisted the collapsed state and width ratio in browser storage.
- [x] Kept the existing mobile Source/Canvas tabs independent from the desktop panel layout.
- [x] Expanded “Show in source” so it restores a collapsed source panel before revealing a node.
- [x] Localized panel controls in English, Turkish, Spanish, German, and French.
- `npm run check`: passed with 20 unit tests.
- `npm run test:e2e`: 17 browser tests passed, including resize persistence and collapse/restore coverage.

## Double-click box editing

- [x] Opened the visual editor with the existing box values on canvas double-click.
- [x] Made title, supporting text, prepared answer, relevant property, type, color, shape, and status editable.
- [x] Wrote visual changes back to the matching `.mtree` source block.
- [x] Preserved node ids, connections, `@tag`, `@priority`, and other non-visual attributes.
- [x] Kept the new-box parent selector out of edit mode while leaving type and style controls available.
- [x] Localized edit actions in English, Turkish, Spanish, German, and French.
- `npm run check`: passed with 20 unit tests.
- `npm run test:e2e`: 18 browser tests passed, including double-click editing and source-preservation coverage.

## Landing documentation

- [x] Added a two-minute visual onboarding path from view selection to Live Run.
- [x] Added a line-by-line `.mtree` syntax workbench with tree indentation, node anatomy, content attributes, and graph connections.
- [x] Added compact language reference cards for headers, branches, connections, content, and visual style.
- [x] Added accessible interview-tree and binary-search diagram illustrations based on the actual BranchScript interface.
- [x] Added hosted, Node.js, and Docker setup paths with a clear public-frontend/private-backend boundary.
- [x] Added SoftwareApplication and HowTo structured data for search engines.
- [x] Kept documentation responsive, with wide source examples scrolling inside their own panel on mobile.
- `npm run check`: passed with 20 unit tests.
- `npm run test:e2e`: 20 browser tests passed, including landing content, illustration loading, and mobile overflow coverage.
- Desktop and 390×844 browser inspection completed with no console errors.

## Brand mark

- [x] Replaced the plain letter mark with a scalable straw-paper logo.
- [x] Combined a warm yellow paper surface, subtle canvas dots, and a branching thought-tree symbol.
- [x] Kept the mark readable as a favicon and at compact application-header sizes.
- [x] Used the same SVG across the landing page, guides, FAQ, and playground.
- [x] Added a 512×512 transparent PNG for touch icons and external asset use.
- `npm run check`: passed with 20 unit tests.
- Landing and playground Chromium render tests passed; both header sizes were visually inspected.

## Frontend security hardening

- [x] Reviewed DOM write paths, browser storage, imports, cloud requests, Nginx, Docker, Compose, CI, and repository contents for common exposure risks.
- [x] Confirmed the public repository contains no likely credential, private key, database URL, mail secret, or backend implementation.
- [x] Replaced the remaining dynamic diagnostic `innerHTML` write with DOM nodes and `textContent`.
- [x] Added strict schemas and limits for scripts, graph documents, project imports, cloud diagrams, node positions, account inputs, and API responses.
- [x] Added a 15-second request timeout, same-origin credential mode, redirect rejection, no-store caching, JSON content checks, and a 12 MB response ceiling to cloud calls.
- [x] Added a 1 MB import ceiling, 2,000-node ceiling, 5,000-edge ceiling, bounded labels and answers, and aggregate cloud-list limits.
- [x] Tightened CSP with exact JSON-LD hashes, blocked executable script attributes and mixed-content upgrades, and retained the narrowly required inline style-attribute allowance for graph positioning.
- [x] Added transport, browser isolation, clickjacking, MIME, referrer, and feature-policy assertions.
- [x] Made the public static container return 404 for `/api/` so a routing mistake cannot fall through to the SPA.
- [x] Pinned direct npm dependencies, the Node build image, and GitHub Actions versions.
- [x] Changed CI installs to ignore dependency lifecycle scripts and added npm advisory, package-signature, runtime header, HTTP method, API fallback, and unprivileged-user checks.
- [x] Kept import rejection messages visible when a pending local save finishes, so security failures are not silently replaced by a success status.
- [x] Documented the public frontend/private backend boundary and production responsibilities in `SECURITY.md`.

## Validation — security

- `npm audit --audit-level=low`: no known npm advisories found.
- `npm audit signatures`: 165 package signatures and 52 provenance attestations verified after a clean `npm ci --ignore-scripts` install.
- `npm run check`: passed; lint, type checking, 30 unit/configuration tests, and the production build completed.
- `npm run test:e2e`: 21 Chromium tests passed, including literal rendering of an HTML injection payload and oversized import rejection.
- In-app browser inspection found no inline executable script, inline event handler, password value embedded in markup, or browser console error on the local playground.
- `docker compose config --quiet`: passed.
- Local Docker runtime validation could not run because the Docker daemon was unavailable; CI now performs Nginx syntax, runtime UID, security-header, method-restriction, and API fail-closed checks on every change.

## Storage and large canvas follow-up

- [x] Confirmed the private BranchScript backend owns diagram persistence in its own PostgreSQL database; Neuralith remains the identity and mail provider.
- [x] Kept unauthenticated local projects in the frontend's own IndexedDB database.
- [x] Lowered the X6 canvas zoom-out floor from `0.22` to `0.01` and raised the zoom-in ceiling to `16`, so very large interview trees remain navigable.
- [x] Applied the same `0.01` lower bound to Fit view for large documents.
- [x] Moved Command/Control + mouse-wheel zoom into the canvas handler so both modifiers work consistently across browsers and trackpads.
- [x] Prevented node double-click events from affecting canvas navigation; double-click now only opens node editing.
- [x] Added the WHOOP Android Engineer interview tree with opening, question branches, ideal answers, follow-ups, rescue phrases, and closing questions.
- [x] Added a clear Save project action that opens authentication without saving while signed out; cloud save runs only after a completed sign-in and a deliberate save click.
- [x] Added a regression assertion for the large-canvas zoom range and documented the production Compose startup path in `README.md`.
- [x] Prevented dangling edges from being rendered when an endpoint is missing.
- [x] Hid edges whose source or target is outside the current canvas viewport, so large diagrams no longer leave arrowheads in empty space while panning.
- [x] Rechecked edge visibility after zooming, panning, and node movement.
- [x] Changed light-theme node surfaces to use their own palette colors with a soft translucent fill instead of a shared opaque white fill.
- [x] Enabled X6 virtual rendering so large diagrams only keep viewport-relevant cells active.
- [x] Coalesced edge visibility work into animation frames and skipped unchanged visibility writes to reduce zoom and pan stutter.
- [x] Batched modifier-wheel zoom events into one X6 transform per animation frame while preserving accumulated zoom direction and pointer focus.
- [x] Made node double-click center the component and bring it to a readable zoom level before opening its editor.
- [x] Added explicit `@items` and `@fields` syntax for data cells and record rows.
- [x] Reworked Data view cards to render array, stack, queue, linked-list, and record contents as visual cells instead of plain labels.
- [x] Expanded the Data playground and in-app syntax guide with practical structure examples and pointer connections.
- [x] Simplified Add Box into a basic-first form with text, type, and supporting text shown initially.
- [x] Moved answer, feature, connection, color, shape, and status controls behind an Advanced settings toggle.
- [x] Auto-opened advanced settings while editing a node that already uses advanced fields and localized the toggle across supported languages.
- [x] Added a disabled System Design playground card with a clear Coming soon state; it does not enter the current view or parser list yet.

## Deployment and registration audit

- [x] Confirmed the public frontend still contains no backend implementation or deployment secrets.
- [x] Isolated BranchScript accounts from Neuralith's primary users database while preserving Neuralith email delivery.
- [x] Added fail-closed behavior when the isolated auth database is unavailable.
- [x] Added in-app verification-code resend support and service-unavailable feedback.
- [x] Aligned gateway CSP and browser-isolation headers with the static production container.
- [x] Prevented the seventh playground card from overlapping canvas toolbar controls on desktop widths.
- [x] Updated browser assertions to remain valid with viewport-based X6 virtual rendering.

## Canvas focus, Data layout, and fullscreen

- [x] Changed box double-click to select the box, open its visual editor, and then focus it at a fixed readable `1.25×` scale after the X6 double-click cycle completes.
- [x] Kept the focused box in the visible canvas area when the edit panel occupies the right side.
- [x] Removed X6 reference transforms from Data cell labels so array, queue, list, stack, and record values stay inside their own cells.
- [x] Added bounded truncation for long Data values and increased rich Data card heights for descriptions and operation captions.
- [x] Made all seven playground cards share the available dock width without allowing System Design to overlap the workspace.
- [x] Added a Canvas fullscreen control using the browser Fullscreen API, including an exit state and translations in all five interface languages.
- [x] Added browser regression scenarios for Data cell bounds, System Design dock bounds, double-click focus scale, and fullscreen enter/exit.

## Validation — canvas focus and fullscreen

- `npm run check`: passed; lint, TypeScript, 36 unit/configuration tests, and the production build completed.
- `npx playwright test --list`: passed; all 25 browser scenarios were discovered and compiled.
- In-app measurements confirmed all four array values remain inside the Data card and the System Design card remains above the workspace boundary.
- The remaining live browser run was interrupted by the product usage limit; the new fullscreen and delayed-focus browser scenarios are present but were not executed in that final pass.

## Logic focus overflow

- [x] Replaced fixed double-click zoom in Logic and Algorithm views with a neighborhood fit that includes the selected node and its directly connected nodes.
- [x] Accounted for the visible edit panel when calculating the usable canvas width.
- [x] Kept the existing readable `1.25×` focus behavior for the other diagram views.
- [x] Added a browser regression scenario that checks the focused decision, its input, and both outgoing branches stay inside the canvas.
- `npm run check`: passed; lint, TypeScript, 36 unit/configuration tests, and the production build completed.

## Frontend container deployment

- [x] Configured Vite 8 multi-page HTML entries through the top-level `input` option so development and Linux production builds use the same entry graph and contain the root `/index.html` used by Nginx.
- [x] Passed all HTML pages to Vite as explicit paths; Vite preserves each HTML source path in the production output.
- [x] Made the frontend image build fail immediately when the required root `dist/index.html` is missing.
- [x] Matched the verified Linux build context by copying the complete `.dockerignore`-filtered source tree into the build stage.
- [x] Moved the container build workspace from `/app` to `/workspace` to avoid colliding with the `app/index.html` multi-page entry under Vite 8/Rolldown.
- [x] Added a minimal landing module with a real side effect so Vite includes the static homepage in the production output.
- [x] Moved the production build to a dedicated `/srv/branchscript` document root so the image cannot serve the base Nginx welcome page.
- [x] Copied the build output through a temporary final-stage directory before moving it into the dedicated document root, avoiding base-image document-root behavior.
- [x] Kept the container on the unprivileged `101:101` runtime user.
- `npm run build`: passed; the production output contains the BranchScript landing page and playground assets.

## Source editor and blank projects

- [x] Replaced the fixed `interview.mtree` label with the active source filename.
- [x] Added distinct filenames for every playground example and preserved the filename in local and exported project data.
- [x] Reset the editor selection and scroll position when loading an example, cloud diagram, import, or blank project.
- [x] Added a responsive Blank project card with translations in all five interface languages.
- [x] Allowed explicitly declared diagrams with zero nodes so a blank canvas remains valid and can be populated through Add Box.
- [x] Exported `.mtree` files now use the visible source filename.
- `npm run check`: passed; lint, TypeScript, 37 unit/configuration tests, and the production build completed.
- Focused Chromium regression test for filename, scroll reset, blank canvas, and adding its first box: passed.
- Full Chromium suite: 21 passed; six existing visual/editor interaction scenarios remain failing independently of this change and need a separate stabilization pass.

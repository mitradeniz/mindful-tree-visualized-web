# BranchScript progress — 2026-08-28

## Scope

Build the first complete local-first BranchScript milestone without React or JSX. The application uses permissively licensed dependencies and treats `.mtree` source as the primary document.

## Progress

- [x] Confirmed the product name as BranchScript.
- [x] Selected the vanilla TypeScript architecture and permissive dependency policy.
- [x] Added the initial Vite, TypeScript, lint, test, and build configuration.
- [x] Added the public repository README and `.gitignore`.
- [x] Implemented the graph domain model and Zod validation.
- [x] Defined the `.mtree` 0.1 language and public language guide.
- [x] Added the Lezer grammar and TypeScript semantic compiler.
- [x] Added compiler tests and a representative interview script.
- [x] Built the X6 canvas with pan, zoom, selection, minimap, search, and node-position history.
- [x] Added Dagre layout in left-to-right and top-to-bottom directions.
- [x] Connected CodeMirror to live graph compilation and source-linked diagnostics.
- [x] Added native IndexedDB persistence and `.mtree`/project import-export.
- [x] Added responsive layout, keyboard controls, themes, empty/error states, and source/canvas selection linking.
- [x] Added GitHub Actions, Playwright scenarios, architecture notes, and contribution guidance.
- [x] Completed the final unit, browser, lint, type, and production-build validation.

## Visual playground expansion

- [x] Added `tree`, `flow`, `neural`, and `logic` diagram views to `GraphDocument`.
- [x] Extended `.mtree` with `@view`, explicit `connect` statements, and labeled transitions.
- [x] Added input, layer, neuron, process, decision, outcome, and output node types.
- [x] Allowed intentional loops in flow and logic views while keeping tree and neural views acyclic.
- [x] Added four ready-to-edit scripts to the playground gallery.
- [x] Added view-aware layouts, node shapes, edge routes, colors, and transition labels.
- [x] Added a live runner that traverses branches and highlights the active canvas path.
- [x] Added editor completion entries for the expanded language.
- [x] Documented the new views and connection syntax.
- [x] Added unit coverage for all presets, labels, and cycle rules.
- [x] Added a browser scenario for loading and running the logic template.
- [x] Fixed stale X6 SVG nodes remaining after switching templates.

## Fast authoring expansion

- [x] Added `step`, `choice`, and `result` shorthand declarations.
- [x] Added `@color`, `@shape`, and `@status` node attributes.
- [x] Added custom card, pill, diamond, and circle rendering overrides.
- [x] Added a source-generating visual builder for boxes and connections.
- [x] Added automatic, collision-safe ids for visually created boxes.
- [x] Added canvas double-click and `Ctrl/Cmd + Enter` quick-add shortcuts.
- [x] Added an in-app four-step syntax guide and quick reference.
- [x] Simplified the workspace with a neutral GPT-inspired palette.
- [x] Changed editor syntax colors to a lower-contrast Gruvbox palette.
- [x] Added unit and browser coverage for the new authoring paths.

## Web deployment

- [x] Added a multi-stage Docker build with no Node.js in the runtime image.
- [x] Added the official unprivileged Nginx runtime on port 8080.
- [x] Added a read-only, capability-free Compose service.
- [x] Added container health checks and conservative resource limits.
- [x] Added static SPA routing, safe cache rules, gzip, and method restrictions.
- [x] Added a strict CSP and security response headers without external script origins.
- [x] Disabled production source maps.
- [x] Added Dependabot coverage and CI validation for the production container.

## Mobile workspace

- [x] Added full-height Source and Canvas tabs for phone and tablet layouts.
- [x] Preserved the visual canvas as the default mobile working view.
- [x] Made visual builder and syntax guide actions switch to the visible canvas automatically.
- [x] Added dynamic viewport height and safe-area spacing.
- [x] Converted visual builder forms to one-column, touch-sized controls on phones.
- [x] Reduced the mobile toolbar to search, add, and live-run essentials.
- [x] Adjusted runner, inspector, status, preset cards, and side panels for narrow screens.
- [x] Separated canvas gestures so dragging pans, `Command` + left-drag selects an area, and wheel zoom uses smaller steps.
- [x] Mirrored single and multi-node canvas selections as safe source-line highlights in CodeMirror.

## Validation

- `npm run lint`: passed.
- `npm run typecheck`: passed.
- `npm test`: 11 compiler and preset tests passed.
- `npm run test:e2e`: 7 Chromium scenarios passed.
- `npm run build`: passed and emitted `dist/licenses.md`.
- `npm audit --omit=dev --audit-level=high`: no known vulnerabilities.
- `docker compose config --quiet`: passed.
- `git diff --check`: passed.
- Dependency installation reported no known vulnerabilities.

The production bundle is approximately 330 kB compressed. X6 and CodeMirror are both required on the initial working surface, so bundle splitting is deferred until a measured loading problem appears.

## Cloud account interface

- [x] Added email registration, six-digit verification, and login to the workbench.
- [x] Added a responsive account drawer and private cloud diagram library.
- [x] Preserved local IndexedDB saving and project export as account-free options.
- [x] Added a same-origin browser client for account and diagram operations.
- [x] Kept credentials and cloud implementation outside the public web repository.

## Landing and SEO content

- [x] Moved the working surface to `/app/` and made `/` a static crawlable landing page.
- [x] Added responsive product sections for six visual modes, use cases, workflow, and cloud saving.
- [x] Added a blog index and guides for visual thinking, brainstorming maps, and algorithm visualization.
- [x] Added an expanded FAQ covering scripting, no-code authoring, privacy, algorithms, data structures, mobile use, and self-hosting.
- [x] Added canonical metadata, Open Graph metadata, `robots.txt`, and `sitemap.xml`.
- [x] Added the BranchScript SVG mark and a 1200×630 social preview asset.
- [x] Converted Vite to a multi-page production build while keeping content pages free of JavaScript.

## Algorithms and data structures

- [x] Added `algorithm` and `data` diagram views.
- [x] Added algorithm nodes for start, function, operation, condition, loop, and return.
- [x] Added data nodes for array, item, stack, queue, list, record, and pointer.
- [x] Added dedicated layout, colors, condition diamonds, container dividers, and data-reference rendering.
- [x] Added ready-to-run binary-search and data-structure playground presets.
- [x] Added visual-builder options, editor completion, syntax guide entries, and language documentation.

## Notes

- React and JSX are intentionally excluded.
- `.mtree` source remains independent from visual node coordinates.
- Comments in source code are limited to non-obvious intent.
- The local Docker daemon was not running, so container build validation is delegated to CI.
- The current multi-page `npm run check` passes with 11 compiler/preset tests; production dependencies report no known vulnerabilities.
- The generated social card is stored at `public/branchscript-social.png` at 1200×630.
